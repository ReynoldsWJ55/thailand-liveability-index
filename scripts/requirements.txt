# Copyright (c) 2026 Will Reynolds
# SPDX-License-Identifier: MIT
# Python dependencies for the R2 fetch step in scripts/fetch-scores.py.
# Used by:
#   - GitHub Actions (the pages-deploy workflow installs these before running fetch-scores.py)
#   - Local dev when CLOUDFLARE_ACCOUNT_ID + R2_ACCESS_KEY_ID + R2_SECRET_ACCESS_KEY are set
#
# Without these env vars, fetch-scores.py falls through to src/data/scores.sample.json
# and these deps aren't strictly required.

boto3>=1.34
duckdb>=1.0
python-dotenv>=1.0  # optional — used if CI loads .env files; harmless otherwise
