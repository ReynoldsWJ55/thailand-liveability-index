# Cloudflare R2 credentials for the build-time scores fetch.
# Copy to `.env` (gitignored) and populate locally; CI reads these from
# GitHub Actions secrets, not from the repo.

CF_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
